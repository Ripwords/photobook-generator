{
  "stream": [
    {
      "kind": "head",
      "status": 200,
      "headers": [
        ["content-type", "text/event-stream; charset=utf-8"],
        ["vary", "origin"],
        ["vary", "accept-encoding"]
      ]
    },
    { "kind": "chunk", "bytes": [100, 97, 116, 97, 58, 32, 226, 130] },
    { "kind": "chunk", "bytes": [172, 10, 10] },
    { "kind": "end" }
  ],
  "failed": { "kind": "failed", "message": "error decoding response body: connection reset" },
  "cancelled": { "kind": "cancelled" },
  "errors": [
    { "kind": "missingKey", "provider": "deepseek", "message": "No DeepSeek API key is set. Add it in Settings." },
    { "kind": "missingKey", "provider": "jev", "message": "No Jev API key is set. Add it in Settings." },
    { "kind": "refused", "message": "\"/v1/models\" is not a DeepSeek endpoint this app calls." },
    { "kind": "keychain", "message": "The keychain could not be read." }
  ]
}
