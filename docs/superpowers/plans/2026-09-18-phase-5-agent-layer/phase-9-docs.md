# Phase 9: manual and status

[Overview](overview.md)

## Goal

The README's "Using the app" describes the chat panel, approvals and keys. PROJECT-STATUS
records what is built, what is verified, and what is only assumed.

## Changes

- README: a "Talking to the book" section, key setup, what is sent to DeepSeek and Jev and
  what never is.
- PROJECT-STATUS: the Phase 5 row, and the unmeasured `ROUTE_CONFIDENCE`. Also that Jev's
  retention policy has no zero-retention option for this account.
- The design's §4.1 and §9 each gain a pointer to this plan's deviations table.

## Verification

- Every control named in the README exists under that name in the UI.
