export default defineAppConfig({
  ui: {
    colors: {
      // Black and white. `--ui-primary` is pinned to neutral-900 / neutral-50
      // in main.css, so a primary button is black in light and white in dark.
      primary: "neutral",
      neutral: "neutral",
    },
  },
});
